const modal = grid.createBox({
    name: "modalPanel",
    left: 4,
    right: 4,
    top: 5,
    bottom: 3,
    centred: true
    })
          modal.hidden()
          modal.show()
          modal.setBgColor()